## Model Checkpoints

Trained model weights are not included in this repository.

Model checkpoints are generated during training and saved locally.

To reproduce trained models, run:

python src/train_signwordnet_synthetic.py

The best-performing checkpoint is selected based on validation accuracy.
